apiVersion: v1
kind: Service
metadat:
    name: result-service
    labels:
        name: result-service
        app: demo-voting-app
specs:
    type: : LoadBalancer
    ports:
    - port: 80
      targetPort: 80
    selector:
        name: result-app-pod
        app: demo-voting-app